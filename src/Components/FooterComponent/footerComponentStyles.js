import { StyleSheet } from "react-native";
const styles = StyleSheet.create({
  footerContainer: {
    backgroundColor: "#fff",
    borderWidth: 1,
    borderColor: "blue",
  },
  subText: {
    fontSize: 8,
  },
});

export default styles;
